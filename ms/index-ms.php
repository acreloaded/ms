<h1>AssaultCube Reloaded Master Server</h1>
<p>Use <a href="/cube" >/cube</a> for CubeScript server list</p>
<p>Use <a href="/json" >/json</a> for JSON server list</p>
<p>Use <a href="/bans" >/bans</a> for a verbose ban list</p>
<p>Your server may register any port between <?=$config['servers']['minport']?> and <?=$config['servers']['maxport']?> if:</p>
<p>your server is running protocol <?=$config['servers']['minprotocol']?> or later.</p>